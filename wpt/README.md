# WPT assets

- `manifests/`: lists of WPT files to run (relative to `third_party/wpt`).
- `expectations/`: expected failures used to classify results.
- `baselines/`: committed reference results for regression comparison.
- `results/`: local run artifacts (ignored by git).

